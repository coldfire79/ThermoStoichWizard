#
# define display information
#
name: Build Stoichiometric Reaction Models From Chemical Formulas

tooltip: |
    Build stoichiometric reaction models from the chemical compositions

screenshots: []

icon: icon.png

#
# define a set of similar apps that might be useful to the user
#
# suggestions:
#     apps:
#         related:
#             [app1, app2]
#         next:
#             [app3, app4]

#
# Configure the display and description of parameters
#
parameters :
    input_tbl :
        ui-name : |
            FT-ICR Data
        short-hint : |
            FT-ICR Data imported to KBaseExperiments.AttributeMapping
        long-hint  : |
            FT-ICR Data imported to KBaseExperiments.AttributeMapping format. It should contain the molecular fomular and concentrations.

    n_lambda_bins :
        ui-name : |
            The number of lambda bins
        short-hint : |
            The number of lambda bins to sample reactions
        long-hint  : |
            The number of lambda bins to sample reactions

    lambda_cutoff :
        ui-name : |
            The cutoff % in the lambda distribution
        short-hint : |
            The cutoff % of the tails in the lambda distribution
        long-hint  : |
            The cutoff % of the tails in the lambda distribution
    
    output_surfix :
        ui-name : |
            Surfix for output objects
        short-hint : |
            The surfix string for the output objects (FBA models and Media)
        long-hint  : |
            The surfix string for the output objects (FBA models and Media)

description : |
    <p>This KBase app is to build the stoichiometric reaction models (stoichiometry matrices) from from a given set of compounds.</p>

